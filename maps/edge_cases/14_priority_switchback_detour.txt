# Edge Case 14: Priority Switchback Detour
# Tests Dijkstra cost-parity load balancing between a short normal corridor and a longer priority switchback.
# Path Direct: start -> direct1 -> goal (2 normal hops @ cost 10 each = cost 20).
# Path Switchback: start -> p1 -> p2 -> p3 -> goal (4 priority hops @ cost 5 each = cost 20).
# Both paths have identical base Dijkstra cost (20), but different physical lengths and capacities.
# Tests concurrent allocation across paths with identical costs but distinct hop counts.

nb_drones: 6

start_hub: start 0 0 [color=green]
hub: direct1 2 0 [zone=normal color=blue max_drones=1]
hub: p1 1 2 [zone=priority color=cyan max_drones=2]
hub: p2 2 2 [zone=priority color=cyan max_drones=2]
hub: p3 3 2 [zone=priority color=cyan max_drones=2]
end_hub: goal 4 0 [color=green]

# Direct narrow path
connection: start-direct1 [max_link_capacity=1]
connection: direct1-goal [max_link_capacity=1]

# Priority switchback bypass
connection: start-p1 [max_link_capacity=2]
connection: p1-p2 [max_link_capacity=2]
connection: p2-p3 [max_link_capacity=2]
connection: p3-goal [max_link_capacity=2]
