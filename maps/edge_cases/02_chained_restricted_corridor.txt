# Edge Case 2: Chained Restricted Corridor
# Tests sequential chained restricted zones where drones must caterpillar through consecutive 2-turn hops.
# Every hub along the single corridor is restricted with max_drones=1.
# Tests that departure from one restricted zone and entry into the next correctly updates occupancies
# without causing deadlocks, race conditions, or illegal overlaps.

nb_drones: 4

start_hub: start 0 0 [color=green]
hub: pipe1 1 0 [zone=restricted color=red max_drones=1]
hub: pipe2 2 0 [zone=restricted color=red max_drones=1]
hub: pipe3 3 0 [zone=restricted color=red max_drones=1]
end_hub: goal 4 0 [color=green]

connection: start-pipe1 [max_link_capacity=1]
connection: pipe1-pipe2 [max_link_capacity=1]
connection: pipe2-pipe3 [max_link_capacity=1]
connection: pipe3-goal [max_link_capacity=1]
